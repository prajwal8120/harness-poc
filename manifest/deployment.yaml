apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-addressbook
  namespace: harness-poc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-addressbook
  template:
    metadata:
      labels:
        app: my-addressbook
    spec:
      containers:
        - name: my-addressbook-app
          image: insta7120/harness-poc-demo:v1.0
          ports:
            - containerPort: 8080
